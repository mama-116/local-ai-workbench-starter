# Local AI Workbench Starter - Agent Guide

このリポジトリは、一般ユーザーがCodexとの会話を通じてLocalLLMアプリを開発するための補助輪環境である。

## 最優先の振る舞い

- 依頼が曖昧なら実装前にヒアリングする。質問は一度に最大3問、最大2ラウンドとする。
- 利用者へ複数ステップの宿題を渡さない。次の一手は原則1つ、30分以内にする。
- 専門用語を使う場合は、直後に利用者の目的へ結び付けて説明する。
- 意見が分かれる判断は、推奨案だけでなく最も強い反論と見直し条件を示す。
- UIを決めるときは、理由の異なる3案を提示し、可能なら各案のモックを生成する。選択前に本実装を始めない。
- GitHub Issueは内部台帳として扱える。利用者にIssue操作を強制しない。
- ゴールが変わる、独立した成果物へ移る、または引き継ぎなしでは文脈維持が難しい場合にだけ新しいスレッドを提案する。

## 正本

- 要件: `docs/REQUIREMENTS.md`
- アーキテクチャと境界: `docs/ARCHITECTURE.md`
- 意思決定: `docs/adr/`
- 会話引き継ぎ: `docs/templates/HANDOFF_TEMPLATE.md`

同じ定義を複数文書へコピーしない。状態値、承認区分、データ境界は `docs/ARCHITECTURE.md` を正本とする。

## 開発順

契約、壊れるシナリオのテスト、実装の順で進める。各フェーズは単独で動作確認できる状態で終える。

1. 配布キットと環境診断
2. Ollamaチャットとログ保存
3. モデル・キャラクター切替とリソース表示
4. RAG
5. 読み取り専用ツールとMCP
6. 定期実行
7. 制限付き自律実行
8. WSL2上のvLLMなど推論基盤の追加

## セキュリティ

- 会話本文、RAG資料、個人アーカイブを外部APIへ送信しない。
- Web検索は抽出・確認済みの検索語だけを送る。SearXNGを優先し、DuckDuckGoは明示設定時だけ使う。
- 外部投稿、外部削除、課金操作は毎回人間の承認を得る。
- ローカル書き込みは許可されたワークスペース内に限定する。
- APIキー、トークン、`.env`、個人ログをGitへ追加しない。

## Git

- `docs/GITHUB_FLOW.md` をGitHub Flow運用の正本とする。
- すべての変更は最新の `main` から短命な作業ブランチを作り、Pull Requestを経由する。`main`へ直接commitまたはpushしない。
- Codexの作業ブランチは `agent/<短い目的>` とし、コミットは完了条件単位にする。
- 無関係な変更をstageせず、PR前にテスト、型検査、秘密情報検査、差分レビューを実行する。
- 原則Draft PRから始め、検査とレビューが完了するまでReadyまたはmergeにしない。
- Issueやブランチ操作は、利用者には目的と結果を日本語で説明する。

## 完了報告

結果、確認した内容、残る未決事項、次の一手を簡潔に伝える。検証していないことを完了扱いしない。
